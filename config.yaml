# ============================================================
# ProjectMgr 全局配置文件
# ============================================================
# 临时文件根目录 — 所有上传、解压、输出文件均存放于此，
# 避免与项目源码混淆。
tmp_dir: /Users/peyton/tmp

# 当 tmp_dir 不存在时是否自动创建
auto_create: true
